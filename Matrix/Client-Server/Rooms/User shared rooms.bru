meta {
  name: User shared rooms
  type: http
  seq: 26
}

get {
  url: {{protocol}}://{{delegatedURL}}/_matrix/client/v1/mutual_rooms?user_id=@user:domain.tld
  body: none
  auth: inherit
}

params:query {
  user_id: @user:domain.tld
}
