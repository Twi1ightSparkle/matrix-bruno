meta {
  name: Delist room
  type: http
  seq: 9
}

delete {
  url: {{protocol}}://{{delegatedURL}}/_matrix/client/r0/directory/list/room/:roomId
  body: none
  auth: inherit
}

params:path {
  roomId: {{roomId}}
}
