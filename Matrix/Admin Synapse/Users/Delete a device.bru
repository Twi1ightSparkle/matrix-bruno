meta {
  name: Delete a device
  type: http
  seq: 22
}

delete {
  url: {{protocol}}://{{delegatedURL}}/_synapse/admin/v2/users/:userId/devices/:deviceId
  body: none
  auth: inherit
}

params:path {
  deviceId: Elements Script
  userId: {{userId}}
}
