meta {
  name: Room aliases
  type: http
  seq: 13
}

get {
  url: {{protocol}}://{{delegatedURL}}/_matrix/client/v3/rooms/:roomId/aliases
  body: none
  auth: bearer
}

params:path {
  roomId: {{roomId}}
}

auth:bearer {
  token: {{adminToken}}
}
