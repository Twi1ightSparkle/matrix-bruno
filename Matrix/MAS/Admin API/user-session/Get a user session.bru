meta {
  name: Get a user session
  type: http
  seq: 2
}

get {
  url: {{protocol}}://{{masAdminEndpoint}}/api/admin/v1/user-sessions/:masUserSessionId
  body: none
  auth: inherit
}

params:path {
  masUserSessionId: {{masUserSessionId}}
}
