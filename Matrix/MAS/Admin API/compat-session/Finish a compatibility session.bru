meta {
  name: Finish a compatibility session
  type: http
  seq: 3
}

post {
  url: {{protocol}}://{{masAdminEndpoint}}/api/admin/v1/compat-sessions/:masCompatSessionId/finish
  body: none
  auth: inherit
}

params:path {
  masCompatSessionId: {{masCompatSessionId}}
}
